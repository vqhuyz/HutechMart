<ul class="nav navbar-nav">
    <li class="active"><a href="#">Bán hàng <span class="sr-only">(current)</span></a></li>
    <li class="dropdown user user-menu">
        <!-- Menu Toggle Button -->
        <a href="#" class="dropdown-toggle" data-toggle="dropdown">
            <!-- The user image in the navbar-->
            <img src="~/Img/NhanVien/@Session["HinhAnh"]" class="user-image">
            <!-- hidden-xs hides the username on small devices so only the image appears. -->
            <span class="hidden-xs">@Session["TenNV"]</span>
        </a>
        <ul class="dropdown-menu">
            <!-- The user image in the menu -->
            <li class="user-header">
                <img src="~/Img/NhanVien/@Session["HinhAnh"]" class="img-circle">
                <p>
                    <small>@Session["ChucVu"]</small>
                    @Session["TenNV"]
                </p>
            </li>
            <li class="user-footer">
                <div class="pull-left">
                    <a href="@Url.Action("ThongTin","NhanVien", new { id = Session["NhanVien"]})" class="btn btn-default btn-flat">Profile</a>
                </div>
                <div class="pull-right">
                    <a href="@Url.Action("Logout","Login")" onclick="return confirm('Bạn đang tiến hành đăng xuất khỏi tài khoản');">
                        <i class="fa fa-sign-out"></i> <span>Sign Out</span>
                    </a>
                </div>
            </li>
        </ul>
    </li>
</ul>
